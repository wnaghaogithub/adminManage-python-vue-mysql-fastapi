# ---- Web 框架与服务器 ----
fastapi>=0.115.0          # 高性能 Web 框架，提供路由、请求参数校验、Swagger 文档
uvicorn[standard]>=0.30.0 # ASGI 服务器，用于运行 FastAPI 应用（[standard] 附带热重载等能力）

# ---- 数据库 ORM 与驱动 ----
sqlalchemy>=2.0.0         # ORM 框架，用 Python 对象映射数据库表（本项目 users / admins 表）
pymysql>=1.1.0            # MySQL 的纯 Python 驱动，SQLAlchemy 通过它连接 MySQL

# ---- 加密与鉴权 ----
cryptography>=42.0.0      # 加密库，用 Fernet 对用户密码做可逆加密（满足前端切换显示明文）
PyJWT>=2.8.0              # 生成 / 校验 JWT Token，用于登录鉴权

# ---- 请求处理 ----
python-multipart>=0.0.9   # 解析 multipart 表单数据，支撑头像文件上传接口
pydantic>=2.7.0           # 数据校验库，定义请求 / 响应模型（app/schemas.py）
